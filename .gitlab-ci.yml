stages:
    - test

test:cargo:
    image: "rust:latest"
    stage: test
    script:
        - rustc --version && cargo --version
        - cargo check
        - cargo check --features encoding
        - cargo check --features rt-tokio
        - cargo check --features rt-client
        - cargo test --features encoding,rt
